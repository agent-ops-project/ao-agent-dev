# Test server communication

1. Start server
2. Run `simulate_shim.py` (dummy shim where you can implement some messaging logic)
3. In seperate terminal: run `simulate_ui.py` (dummy UI where you can implement what the UI sends the server)